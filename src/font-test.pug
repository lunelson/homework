extends app/_app

append defs
  - title = 'Test: Font'

append head
  style.

    *, *::before, *::after {
      box-sizing: border-box;
    }

    .label::before {
      font-family: monospace;
      //- font-size: 1rem;
      counter-reset: cp var(--stack-gap);
      counter-reset: cp ;
      content: counter(cp);
      display: block;
      width: 100%;
      padding: 1em;
      background-color: #ddd;
    }

block body
  .wrap-last
    .frame
      .stack-l
        .stack-m
          .f-sans-m= chance.sentence()
          .bleed-right
            .label
          .label
          .stack-s
            .f-sans-m= chance.sentence()
            .f-sans-s= chance.sentence()
          .label
          .label
          .f-sans-m= chance.sentence()
          .f-sans-s= chance.sentence()
          .label
          .label
          .f-sans-m= chance.sentence()
          .f-sans-s= chance.sentence()
          .label
          .label
          .f-sans-m= chance.sentence()
          .f-sans-s= chance.sentence()
          .label
          .label
